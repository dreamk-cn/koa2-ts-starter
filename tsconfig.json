{
  "compilerOptions": {
    "target": "ES2022",
    "module": "Node16",
    "moduleResolution": "Node16",
    
    /* --- 路径别名配置，需要配合tsc-alias --- */
    "paths": {
      "@/*": ["./src/*"]
    },
    
    "outDir": "./dist",
    "rootDir": "./src",
    
    /* 严格检查 */
    "strict": true,
    "noImplicitAny": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "experimentalDecorators": true, // 允许使用装饰器
    "emitDecoratorMetadata": true, // 生成元数据，TypeORM 必须用这个
    "strictPropertyInitialization": false // 关闭严格属性初始化, class属性不需要在 constructor 里赋值
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"],

  /* 这会让 nodemon/ts-node 在开发运行时自动识别 @/ */
  "ts-node": {
    "require": ["tsconfig-paths/register"]
  }
}